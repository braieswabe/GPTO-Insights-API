{
  "name": "gpto-insights-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GPTO Insights API Gateway – pre-computed dashboard read-model service",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "check-env": "node scripts/check-env.js",
    "test": "node --test tests/*.test.js",
    "type-check": "node --check src/server.js && node --check src/*.js && node --check src/builders/*.js && node --check scripts/*.js && node --check api/index.js && node --check 'api/[...path].js'"
  },
  "dependencies": {
    "postgres": "^3.4.8"
  },
  "engines": {
    "node": "20.x"
  }
}
